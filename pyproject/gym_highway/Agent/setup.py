from setuptools import setup

setup(name='gym_highway',
      version='0.0.1',
      install_requires=['gym', 'lib', 'sklearn', 'scikit-learn', 'matplotlib', 'numpy']  # And any other dependencies gearbox needs
      )