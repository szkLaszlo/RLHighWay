from setuptools import setup

setup(name='sumoProject',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'torch', 'matplotlib', 'tb-nightly', 'easygui', 'tensorboard']
      # And any other dependencies required
      )
