from setuptools import setup

setup(name='sumoProject',
      version='0.0.1',
      install_requires=['gym', 'tensorflow', 'numpy', 'torch', 'easygui']  # And any other dependencies required
      )
